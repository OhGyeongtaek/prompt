# OGT Advisor — 전략 + 자동 오케스트레이션

사용자의 작업 요청을 받아, Opus advisor에게 전략적 판단을 받고 코드 작성은 `/codex`에 위임하며, 작업 완료까지 이 루프를 자동으로 반복한다.

## 사용자 요청

$ARGUMENTS

## 역할 분담

```
메인 세션 (Orchestrator): 컨텍스트 수집, 흐름 제어, 테스트 실행, 결과 통합
Advisor (Opus 서브에이전트): 설계 방향, 에러 분석, 접근법 판단, 최종 검증
Codex (/codex 스킬): 코드 작성, 코드 수정
```

## 핵심 원칙: 자동 진행

**이 스킬이 호출되면 Phase 1부터 Phase 5까지 사용자의 추가 입력 없이 자동으로 끝까지 진행한다.**

- Phase 간 전환 시 사용자에게 묻지 않는다. 상태만 한 줄로 보고하고 즉시 다음 Phase로 넘어간다.
- Advisor 조언을 받으면 즉시 Codex 호출로 이어간다. 조언을 보여주고 멈추지 않는다.
- 사용자 확인이 필요한 유일한 시점은 Phase 5 완료 후 최종 결과 보고 때뿐이다.
- 에러가 발생해도 자동으로 Advisor에게 재조언을 받고 수정을 이어간다.

## 실행 루프

아래 루프를 작업이 완료될 때까지 멈추지 않고 반복한다.

### Phase 1: Orientation (컨텍스트 수집)

코드를 작성하기 전에 먼저 현재 상태를 파악한다:

- 사용자의 요청 내용 (`$ARGUMENTS`) 분석
- 관련 파일 탐색 및 읽기 (Glob, Grep, Read)
- git diff로 현재 변경사항 확인
- 기존 코드의 패턴, 컨벤션, 의존성 파악

이 단계에서는 코드를 작성하지 않는다. 탐색만 한다.

### Phase 2: Advisor 호출 — 방향 설정

Agent 도구로 Opus advisor 서브에이전트를 호출한다.

**서브에이전트 설정:**
- `model`: `"opus"`
- `description`: 작업에 맞는 3-5 단어 요약

**브리핑 프롬프트 구성:**

1. **현재 상황**: Phase 1에서 파악한 프로젝트 상태, 관련 코드, 파일 구조
2. **목표**: 사용자가 원하는 결과물
3. **질문**: 이 시점에서 advisor에게 묻는 구체적 질문
   - 첫 호출: "이 작업의 설계 방향과 구현 순서를 잡아줘"
   - 에러 시: "이 에러의 원인과 해결 접근법을 판단해줘"
   - 완료 전: "이 구현이 올바른지 검증해줘"
4. **제약 조건**: 기술 스택, 아키텍처 규칙, 성능 요구사항

프롬프트 끝에 반드시 포함:

```
응답 규칙:
- 100단어 이내로 간결하게 답변한다
- 번호 매긴 액션 아이템으로 구성한다
- 설명이 아닌 판단과 방향을 제시한다
- 근거가 있다면 한 줄로 이유를 덧붙인다
- 리스크나 주의사항이 있으면 마지막에 명시한다
```

### Phase 3: Codex 위임 — 코드 작성

**메인 세션이 직접 코드를 작성하지 않는다.** 반드시 Bash 도구로 `codex exec` 명령어를 실행하여 Codex에게 위임한다. 이렇게 하면 코드 생성 토큰이 OpenAI 쪽으로 분산된다.

**호출 방법:** Bash 도구로 `codex exec`를 실행한다:

```bash
codex exec "구체적인 작업 지시를 여기에 작성한다"
```

**실제 예시:**
```bash
codex exec "src/backtest/engine.py 파일에 EventDrivenEngine 클래스를 생성해줘. BaseEngine을 상속하고, run() 메서드에서 이벤트 루프를 구현해야 한다. 기존 코드 패턴은 src/strategy/base.py의 ABC 패턴을 따른다."
```

**codex exec 프롬프트에 포함할 내용:**
- Advisor가 제시한 구현 계획 중 현재 단계
- 대상 파일 경로
- 따라야 할 코드 패턴 및 컨벤션 (구체적으로 — Codex는 프로젝트 컨텍스트를 모를 수 있음)
- 구체적인 구현 범위 (한 번에 하나의 명확한 단위)

**Codex 호출 원칙:**
- **절대로 Edit, Write 도구로 직접 코드를 작성하지 않는다** — 코드 작성은 모두 `codex exec`를 통한다
- 한 번에 하나의 파일 또는 하나의 기능 단위만 요청한다
- Advisor의 계획에서 벗어나는 작업은 요청하지 않는다
- `codex exec` 실행 후 작성된 코드를 Read로 반드시 확인한 후 다음 단계로 진행한다
- Codex 실행이 오래 걸릴 수 있으므로 `timeout: 300000` (5분)을 설정한다

### Phase 4: 검증

Codex가 작성한 코드를 검증한다:

- 작성된 코드를 읽고 Advisor 계획과 일치하는지 확인
- 테스트 실행 (가능한 경우)
- 린트/타입 체크 실행 (가능한 경우)

**검증 결과에 따른 분기:**

- **통과** → 다음 구현 단계가 있으면 Phase 3로, 모든 단계 완료면 Phase 5로
- **실패** → Phase 2로 돌아가 Advisor에게 에러 컨텍스트와 함께 재조언 요청

### Phase 5: Advisor 최종 검증

모든 구현이 끝나면 Advisor를 한 번 더 호출한다.

**최종 검증 브리핑:**
- 완성된 코드의 전체 diff
- 테스트 결과
- 원래 요청 대비 구현 범위

Advisor가 문제를 지적하면 Phase 3으로 돌아가 수정한다.
문제가 없으면 사용자에게 결과를 보고하고 종료한다.

## Advisor 호출 타이밍 규칙

자동으로 Advisor를 호출하는 시점:

1. **작업 시작 직후** — orientation 완료 후, 코드 작성 전 (필수)
2. **에러 반복** — 같은 에러가 2회 이상 발생할 때
3. **접근법 변경 고려** — 현재 방향이 막혔다고 판단될 때
4. **작업 완료 직전** — 최종 검증 (필수)

호출하지 않는 시점:
- 단순한 파일 읽기/탐색
- Advisor 계획대로 순조롭게 진행 중일 때
- 이미 명확한 다음 단계가 있을 때

## 모델 설정 권장사항

이 스킬은 메인 세션을 **Sonnet**으로 실행할 때 가장 비용 효율적이다.

```
권장 구성:
  메인 세션: Sonnet  → 오케스트레이션 (저렴한 input/output)
  Advisor:   Opus    → 전략 판단 (비싸지만 소량 output)
  Codex:     별도    → 코드 작성 (Claude 토큰 미사용)
```

스킬 호출 전 `/model sonnet`으로 전환하면 Claude 토큰 비용을 크게 줄일 수 있다.
Opus 메인 세션에서도 동작하지만, 비용 절감 효과가 줄어든다.

## 사용자 보고

각 Phase 전환 시 사용자에게 한 줄로 상태를 알리되, **응답을 끊지 않고 즉시 다음 Phase를 이어서 실행한다:**

```
[Phase 1] 컨텍스트 수집 완료 — Advisor 호출 중...
[Phase 2] Advisor 방향 설정 완료 — 3단계 구현 계획 → Codex 호출 중...
[Phase 3] Codex 1/3 단계 코드 작성 완료 → 검증 중...
[Phase 4] 테스트 통과 → Codex 2/3 단계 진행...
[Phase 5] Advisor 최종 검증 통과 → 작업 완료
```

보고 후 멈추지 않는다. 보고는 진행 상황 표시일 뿐, 사용자 입력 대기점이 아니다.
